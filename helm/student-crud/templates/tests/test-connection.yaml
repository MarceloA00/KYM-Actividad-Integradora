apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "student-crud.fullname" . }}-test-connection"
  labels:
    {{- include "student-crud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "student-crud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
